apiVersion: fullend/v1
kind: Project
metadata:
  name: zenflow
backend:
  lang: go
  framework: gin
  module: github.com/example/zenflow
  middleware:
    - bearerAuth
  auth:
    type: jwt
    secret_env: JWT_SECRET
    claims:
      ID: user_id:int64
      OrgID: org_id:int64
      Role: role
    roles:
      - admin
      - member
frontend:
  lang: typescript
  framework: react
  bundler: vite
  name: zenflow-web
