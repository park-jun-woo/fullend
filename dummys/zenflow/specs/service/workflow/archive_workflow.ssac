package service

// @get Workflow workflow = Workflow.FindByID({ID: request.id})
// @empty workflow "workflow not found"
// @auth "ArchiveWorkflow" "workflow" {ResourceID: workflow.ID} "not authorized"
// @state workflow {status: workflow.Status} "ArchiveWorkflow" "cannot archive"
// @put Workflow.UpdateStatus({ID: workflow.ID, Status: "archived"})
// @get! Workflow updated = Workflow.FindByID({ID: workflow.ID})
// @response { workflow: updated }
func ArchiveWorkflow() {}
