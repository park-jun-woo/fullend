version: "2"
sql:
  - engine: "postgresql"
    schema: "db/"
    queries: "db/queries/"
    gen:
      go:
        package: "db"
        out: "/tmp/phase008-test/backend/internal/db"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_empty_slices: true
