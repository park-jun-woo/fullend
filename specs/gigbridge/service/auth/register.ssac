package service

import "github.com/geul-org/fullend/pkg/auth"

// @get User existingUser = User.FindByEmail({Email: request.email})
// @exists existingUser "Email already registered"
// @call auth.HashPasswordResponse hp = auth.HashPassword({Password: request.password})
// @post User user = User.Create({Email: request.email, PasswordHash: hp.HashedPassword, Role: request.role, Name: request.name})
// @response { user: user }
func Register() {}
