package service

// @get Proposal proposal = Proposal.FindByID({ID: request.id})
// @empty proposal "Proposal not found"
// @get Gig gig = Gig.FindByID({ID: proposal.GigID})
// @empty gig "Gig not found"
// @auth "RejectProposal" "gig" {UserID: currentUser.ID, ResourceID: gig.ClientID} "Not authorized"
// @state proposal {Status: proposal.Status} "RejectProposal" "Cannot reject proposal"
// @put Proposal.UpdateStatus({Status: "rejected", ID: proposal.ID})
// @get Proposal updatedProposal = Proposal.FindByID({ID: proposal.ID})
// @response { proposal: updatedProposal }
func RejectProposal() {}
