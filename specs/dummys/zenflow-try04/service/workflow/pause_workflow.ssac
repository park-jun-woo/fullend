package service

// @auth "PauseWorkflow" "workflow" {ResourceID: request.id} "Not authorized" 403
// @get Workflow wf = Workflow.FindByID({ID: request.id})
// @empty wf "Workflow not found" 404
// @state workflow {status: wf.Status} "PauseWorkflow" "Cannot pause workflow" 409
// @put Workflow.UpdateStatus({ID: wf.ID, Status: "paused"})
// @get Workflow updated = Workflow.FindByID({ID: wf.ID})
// @empty updated "Workflow not found" 404
// @response { workflow: updated }
func PauseWorkflow() {}
