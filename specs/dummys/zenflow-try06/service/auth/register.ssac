package service

import "github.com/park-jun-woo/fullend/pkg/auth"

// @get Organization org = Organization.FindByID({ID: request.org_id})
// @empty org "Organization not found" 404
// @call auth.HashPasswordResponse hp = auth.HashPassword({Password: request.password})
// @post User user = User.Create({OrgID: request.org_id, Email: request.email, PasswordHash: hp.HashedPassword, Role: request.role, Name: request.name})
// @response { user: user }
func Register() {}
