# Auto-generated by fullend gen — do not edit.
# Run: hurl --test --variable host=http://localhost:8080 smoke.hurl


# ===== Organizations =====

# CreateOrganization
POST {{host}}/organizations
Content-Type: application/json
{
  "credits_balance": 1,
  "name": "test_string",
  "plan_type": "test_string"
}

HTTP 201
[Captures]
organization_id: jsonpath "$.organization.id"
[Asserts]
jsonpath "$.organization" exists

# ===== Auth =====

# Register
POST {{host}}/users/register
Content-Type: application/json
{
  "email": "test@test.com",
  "name": "test_string",
  "org_id": {{organization_id}},
  "password": "Password1234!",
  "role": "admin"
}

HTTP 201
[Asserts]
jsonpath "$.user" exists

# Login
POST {{host}}/users/login
Content-Type: application/json
{
  "email": "test@test.com",
  "password": "Password1234!"
}

HTTP 200
[Captures]
token: jsonpath "$.access_token"
[Asserts]
jsonpath "$.access_token" exists


# ===== Templates =====

# PublishTemplate
POST {{host}}/templates
Authorization: Bearer {{token}}
Content-Type: application/json
{
  "category": "test_string",
  "description": "test_string",
  "source_workflow_id": 1,
  "title": "test_string"
}

HTTP 201
[Captures]
template_id: jsonpath "$.template.id"
[Asserts]
jsonpath "$.template" exists


# ===== Webhooks =====

# CreateWebhook
POST {{host}}/webhooks
Authorization: Bearer {{token}}
Content-Type: application/json
{
  "event_type": "test_string",
  "url": "https://example.com/test"
}

HTTP 201
[Captures]
webhook_id: jsonpath "$.webhook.id"
[Asserts]
jsonpath "$.webhook" exists


# ===== Workflows =====

# CreateWorkflow
POST {{host}}/workflows
Authorization: Bearer {{token}}
Content-Type: application/json
{
  "title": "test_string",
  "trigger_event": "test_string"
}

HTTP 201
[Captures]
workflow_id: jsonpath "$.workflow.id"
[Asserts]
jsonpath "$.workflow" exists


# ===== Templates =====

# CloneTemplate
POST {{host}}/templates/{{template_id}}/clone
Authorization: Bearer {{token}}

HTTP 201
[Asserts]
jsonpath "$.template_id" exists
jsonpath "$.workflow" exists


# ===== Workflows =====

# ActivateWorkflow
POST {{host}}/workflows/{{workflow_id}}/activate
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.workflow" exists

# AddAction
POST {{host}}/workflows/{{workflow_id}}/actions
Authorization: Bearer {{token}}
Content-Type: application/json
{
  "action_type": "test_string",
  "payload_template": "test_string",
  "sequence_order": 1
}

HTTP 201
[Captures]
action_id: jsonpath "$.action.id"
[Asserts]
jsonpath "$.action" exists

# CreateWorkflowVersion
POST {{host}}/workflows/{{workflow_id}}/new-version
Authorization: Bearer {{token}}

HTTP 201
[Asserts]
jsonpath "$.workflow" exists

# SetSchedule
POST {{host}}/workflows/{{workflow_id}}/schedule
Authorization: Bearer {{token}}
Content-Type: application/json
{
  "cron": "test_string"
}

HTTP 200
[Asserts]
jsonpath "$.cron" exists
jsonpath "$.next_run" exists

# PauseWorkflow
POST {{host}}/workflows/{{workflow_id}}/pause
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.workflow" exists


# ===== Templates =====

# ListTemplates
GET {{host}}/templates?limit=2&category=test_string

HTTP 200
[Asserts]
jsonpath "$.has_next" exists
jsonpath "$.items" isCollection
jsonpath "$.next_cursor" exists


# ===== Webhooks =====

# ListWebhooks
GET {{host}}/webhooks
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.webhooks" isCollection


# ===== Workflows =====

# ListWorkflows
GET {{host}}/workflows
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.workflows" isCollection


# ===== Templates =====

# GetTemplate
GET {{host}}/templates/{{template_id}}

HTTP 200
[Asserts]
jsonpath "$.org_name" exists
jsonpath "$.template" exists


# ===== Workflows =====

# GetWorkflow
GET {{host}}/workflows/{{workflow_id}}
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.workflow" exists

# ListExecutionLogs
GET {{host}}/workflows/{{workflow_id}}/logs
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.logs" isCollection

# GetSchedule
GET {{host}}/workflows/{{workflow_id}}/schedule
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.cron" exists
jsonpath "$.next_run" exists

# ListWorkflowVersions
GET {{host}}/workflows/{{workflow_id}}/versions
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.versions" isCollection


# ===== Webhooks =====

# DeleteWebhook
DELETE {{host}}/webhooks/{{webhook_id}}
Authorization: Bearer {{token}}

HTTP 200
[Asserts]
jsonpath "$.id" exists

